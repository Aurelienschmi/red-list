<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Information sur le pays</title>
    <link rel="stylesheet" href="/country.css" />
</head>
<body>
    <div class="container">
        <h1>Information sur le pays</h1>
        <div class="info-item">
            <img src="<%= country.flags.svg %>" alt="Drapeau de <%= country.name.common %>">
        </div>
        <div class="info-item">
            <strong>Nom:</strong> <%= country.name.common %>
        </div>
        <div class="info-item">
            <strong>Capitale:</strong> <%= country.capital %>
        </div>
        <div class="info-item">
            <strong>Population:</strong> <%= country.population.toLocaleString() %>
        </div>
        <div class="info-item">
            <strong>Continent:</strong> <%= country.region %>
        </div>
        <div class="info-item">
            <strong>Langue(s) officielle(s):</strong> <%= Object.values(country.languages).join(', ') %>
        </div>
        <div class="info-item">
            <strong>Monnaie:</strong> <%= Object.values(country.currencies).map(c => c.name).join(', ') %>
        </div>
        <div class="button-container">
            <button onclick="window.location.href='/country/<%= country.cca2 %>/endangered'">Voir les espèces menacées</button>
            <button onclick="window.location.href='/'">Choisir un autre pays</button>
        </div>
    </div>
</body>
</html>